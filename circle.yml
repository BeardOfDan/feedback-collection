deployment:
  staging:
    branch: master
    heroku:
      appname: feedbackcollection # appname must not include upper-case characters
  production:
    branch: master
      heroku:
        appname: feedbackcollection # appname must not include upper-case characters
